{
  "name": "userfrosting-monorepo",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/sprinkle-core",
    "packages/sprinkle-account",
    "packages/sprinkle-admin",
    "packages/theme-pink-cupcake",
    "packages/skeleton"
  ],
  "scripts": {
    "vite:dev": "vite",
    "vite:build": "vue-tsc && vite build",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "webpack-dev-server": "^5.2.2"
  }
}
